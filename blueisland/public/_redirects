# SPA fallback — every unmatched route renders the React app.
# NOTE: this file is Netlify/Cloudflare-only; Vercel ignores it as config and
# serves it as a public static asset. Never put private paths in here.
/* /index.html 200
